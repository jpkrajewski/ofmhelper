"""
The standalone one-off tools listed on /helpers: ElevenLabs TTS, radio-comms
audio FX, the Instagram/TikTok scraper. `registry.py` is the list the
`index.py` page renders -- a new helper adds an entry there rather than a new
index template.
"""
