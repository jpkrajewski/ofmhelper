"""
Pulling media in from elsewhere: `videos.py` and `images.py` (bulk
downloaders whose results are grouped per source URL), `clean_image.py` (the
metadata stripper), and `index.py`, the picker page fronting the three.
"""
